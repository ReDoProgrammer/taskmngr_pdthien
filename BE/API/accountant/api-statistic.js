const router = require('express').Router();
const { authenticateAccountantToken } = require("../../../middlewares/accountant-middleware");

// router.get('/job-finished',authenticateAccountantToken,(req,res)=>{

// })

module.exports = router;