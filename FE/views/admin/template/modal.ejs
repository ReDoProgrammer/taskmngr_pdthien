<div id="modalTemplate" class="modal fade" role="dialog">
    <div class="modal-dialog">

        <!-- Modal content-->
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal">&times;</button>
                <h4 class="modal-title">Add or update template</h4>
            </div>
            <div class="modal-body">
                <div class="form-group">
                    <label for="">Name:</label>
                    <input type="text" name="" id="txtName" class="form-control" placeholder="Template name">
                </div>
                <div class="form-group">
                    <label for="">Description:</label>
                    <textarea name="" id="txaDescription" rows="2" class="form-control"
                        placeholder="Template description"></textarea>
                </div>
                <label for="" class="tex-danger font-weight-bold" id="lblTemplateErr"></label>
            </div>
            <div class="modal-footer">
                <button class="btn btn-success" id="btnSubmit">Submit changes</button>
                <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
            </div>
        </div>

    </div>
</div>

<script>
    $('#btnSubmit').click(function () {
        let name = $('#txtName').val();
        let description = $('#txaDescription').val();
        if (name.length == 0) {
            $('#lblTemplateErr').text('(!) Please input template name.')
            $('#lblTemplateErr').slideDown().delay(3000).slideUp();
            return;
        }

        if (tempId.length == 0) {
            $.ajax({
                url: '/admin/template',
                type: 'post',
                headers: {
                    authorization: "Bearer " + localStorage.getItem('accessToken'),
                },
                data: {
                    name,
                    description
                },
                success: function (data) {
                    Swal.fire(
                        'Done!!',
                        data.msg,
                        'success'
                    );
                    $('#modalTemplate').modal('hide');
                },
                error: function (jqXHR, textStatus, errorThrown) {
                    Swal.fire(
                        'Error!!',
                        jqXHR.responseJSON.msg,
                        'error'
                    );
                }
            })
        } else {
            $.ajax({
                url: '/admin/template',
                type: 'put',
                headers: {
                    authorization: "Bearer " + localStorage.getItem('accessToken'),
                },
                data: {
                    tempId,
                    name,
                    description
                },
                success: function (data) {
                    Swal.fire(
                        'Done!!',
                        data.msg,
                        'success'
                    );
                    $('#modalTemplate').modal('hide');
                },
                error: function (jqXHR, textStatus, errorThrown) {
                    Swal.fire(
                        'Error!!',
                        jqXHR.responseJSON.msg,
                        'error'
                    );
                }
            })
        }
    })
</script>